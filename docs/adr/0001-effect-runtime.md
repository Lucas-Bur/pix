# Effect as Runtime and CLI Framework

We're using the Effect ecosystem (effect, @effect/cli, @effect/platform-node) for typed errors, structured concurrency (parallel chunking, serial embedding). Chosen over simpler alternatives like commander because it provides a solid foundation for future growth and typed error handling that aligns with the agent-ready goal.
